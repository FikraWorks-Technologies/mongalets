import {useState} from 'react'
import {Icon} from '../components/Icon'
import type {Role} from '../lib/types'
import './role-profile.css'
const building='https://images.unsplash.com/photo-1564013799919-ab600027ffc6?auto=format&fit=crop&w=1000&q=85',service='https://images.unsplash.com/photo-1621905252507-b35492cc74b4?auto=format&fit=crop&w=1000&q=85'
const map=(q:string)=>`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`
type Item={title:string;meta:string;status?:string;icon?:any;image?:string}
function Layout(p:{eyebrow:string;title:string;text:string;children:React.ReactNode;action?:React.ReactNode}){return <div className="role-section-page"><div className="role-section-head"><div><span className="eyebrow">{p.eyebrow}</span><h1>{p.title}</h1><p>{p.text}</p></div>{p.action}</div>{p.children}</div>}
function Modal(p:{title:string;text?:string;close:()=>void;children:React.ReactNode}){return <div className="ml-modal-backdrop"><div className="ml-modal"><div className="role-panel-head"><div><span className="eyebrow">MongaLets</span><h2>{p.title}</h2><p>{p.text}</p></div><button className="icon-button" onClick={p.close}><Icon name="close"/></button></div>{p.children}</div></div>}
function Field(p:{label:string;icon:any;value:string;set:(v:string)=>void;placeholder:string}){return <label className="ml-access-field"><span>{p.label}</span><div><Icon name={p.icon} size={17}/><input value={p.value} onChange={e=>p.set(e.target.value)} placeholder={p.placeholder}/></div></label>}
function List({items}:{items:Item[]}){const[selected,setSelected]=useState<Item|null>(null);return <>{<div className="role-list">{items.map(x=><button className="role-list-row" key={x.title} onClick={()=>setSelected(x)}><span className="role-list-icon role-tone-info">{x.image?<img src={x.image} alt=""/>:<Icon name={x.icon||'grid'} size={17}/>}</span><div><strong>{x.title}</strong><small>{x.meta}</small></div>{x.status&&<b className="role-status role-status--good">{x.status}</b>}</button>)}</div>}{selected&&<Modal title={selected.title} text={selected.meta} close={()=>setSelected(null)}><div className="role-callout"><Icon name="check" size={18}/><div><strong>Details ready</strong><span>This presentation action opens the full record view.</span></div></div><button className="button button--primary button--full" onClick={()=>setSelected(null)}>Close</button></Modal>}</>}
function Stats({v}:{v:string[][]}){return <div className="role-stats">{v.map(x=><article key={x[0]}><small>{x[0]}</small><strong>{x[1]}</strong><span>{x[2]}</span></article>)}</div>}
function Calendar(){const [event,setEvent]=useState<string|null>(null);const names=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];const cells=Array.from({length:42},(_,i)=>i-5);const events:Record<number,string>={1:'Move-in inspection · 10:00',3:'Cleaning · 09:30',6:'Security review · 14:00',8:'Plumbing visit · 16:00',12:'Resident meeting · 18:00',15:'Gate maintenance · 11:00',20:'Fire drill · 10:30',24:'Service visit · 15:00',28:'Management review · 09:00'};return <Layout eyebrow="Shared calendar" title="Calendar" text="A presentation-ready month calendar with property, access and service activity."><section className="role-panel calendar-panel"><div className="calendar-toolbar"><button className="button button--secondary button--small">‹</button><h2>August 2026</h2><button className="button button--secondary button--small">›</button><button className="button button--primary button--small" onClick={()=>setEvent('Today · 31 August')}>Today</button></div><div className="calendar-grid calendar-head">{names.map(x=><strong key={x}>{x}</strong>)}</div><div className="calendar-grid">{cells.map((n,i)=>{const day=n<1?n+31:n>31?n-31:n;const has=events[day];return <button key={i} className={`calendar-day ${i<5||i>35?'muted':''} ${day===31?'today':''}`} onClick={()=>has&&setEvent(has)}><span>{day}</span>{has&&<small>{has.split(' · ')[0]}</small>}</button>})}</div><div className="calendar-legend"><span>● Access</span><span>● Services</span><span>● Property</span></div></section>{event&&<Modal title={event} text="Calendar event details" close={()=>setEvent(null)}><div className="profile-detail-grid"><div><small>Location</small><strong>Building A · Community area</strong></div><div><small>Time</small><strong>Scheduled property activity</strong></div><div><small>Host</small><strong>Management / property team</strong></div><div><small>Status</small><strong>Confirmed</strong></div></div><button className="button button--primary button--full" onClick={()=>setEvent(null)}>Done</button></Modal>}</Layout>}
function TenantHome(){return <Layout eyebrow="Resident workspace" title="Home" text="Your residence, access, visitors and services in one place."><Stats v={[["Permanent access","Active","ML-A5G-7284"],["Visitors","1 active","Today"],["Services","2 scheduled","This week"]]}/><div className="role-card-grid"><article className="role-photo-card"><img src={building} alt="Building A"/><div><span className="eyebrow">Your residence</span><h3>Building A · Unit A5G</h3><p>Riverside Apartments · Nairobi, Kenya</p><a className="button button--secondary button--small" href={map('Building A Riverside Apartments Nairobi')} target="_blank" rel="noreferrer">View directions</a></div></article><section className="role-panel"><h2>Quick actions</h2><div className="role-button-stack"><Action label="Request a service"/><Action label="Report an issue"/><Action label="View access"/></div></section></div></Layout>}
function Action({label}:{label:string}){const[o,setO]=useState(false);return <><button className="button button--secondary button--full" onClick={()=>setO(true)}>{label}</button>{o&&<Modal title={label} text="Frontend demo action" close={()=>setO(false)}><div className="success-note"><Icon name="check" size={16}/> Action completed successfully in presentation mode.</div></Modal>}</>}
function Visitors(){const[m,setM]=useState(false),[s,setS]=useState(false),[name,setName]=useState(''),[phone,setPhone]=useState('');const link='https://mongalets-one.vercel.app/invite/ML-A5G-7284';const share=(c:string)=>{const t=encodeURIComponent(`Complete your MongaLets visitor details: ${link}`);window.open(c==='WhatsApp'?`https://wa.me/?text=${t}`:c==='Email'?`mailto:?subject=MongaLets visitor invitation&body=${t}`:`sms:?body=${t}`,'_blank')};return <Layout eyebrow="Resident access" title="Visitors" text="Create an invitation or let your guest complete a tenant-linked form."><div className="visitor-choice-grid"><button className="visitor-choice" onClick={()=>setM(true)}><span><Icon name="users" size={22}/></span><div><strong>Add a guest</strong><p>Enter visitor details and create an invitation.</p></div><b>→</b></button><button className="visitor-choice" onClick={()=>setS(true)}><span><Icon name="message" size={22}/></span><div><strong>Share visitor link</strong><p>WhatsApp, Text or Email the secure form.</p></div><b>→</b></button></div><section className="role-panel"><h2>Upcoming visitors</h2><List items={[{title:'Grace Wanjiku',meta:'Today · 2:30–5:00 PM · 1 adult + 1 minor',status:'Active',icon:'users'},{title:'Peter Kamau',meta:'Tomorrow · 11:00 AM · Maintenance guest',status:'Pending',icon:'tool'}]}/></section>{m&&<Modal title="Add a guest" text="Building A · Unit A5G" close={()=>setM(false)}><div className="ml-form-grid"><Field label="Guest full name" icon="users" value={name} set={setName} placeholder="e.g. Grace Wanjiku"/><Field label="Mobile number" icon="phone" value={phone} set={setPhone} placeholder="e.g. 0712 345 678"/></div><div className="role-callout"><Icon name="building" size={18}/><div><strong>Building information is managed by management</strong><span>Riverside Apartments · Building A · Nairobi</span></div></div><button className="button button--primary button--full" disabled={!name||!phone} onClick={()=>setM(false)}>Create guest invitation</button></Modal>}{s&&<Modal title="Share visitor link" text="Your guest fills in their own information." close={()=>setS(false)}><div className="share-link-box"><Icon name="key" size={18}/><span>{link}</span><button className="button button--secondary button--small" onClick={()=>navigator.clipboard?.writeText(link)}>Copy</button></div><div className="share-grid">{['WhatsApp','Text','Email'].map(x=><button key={x} onClick={()=>share(x)}><span className="share-icon">{x==='WhatsApp'?'WA':x==='Text'?'SMS':'@'}</span><strong>{x}</strong><small>Send invitation</small></button>)}</div></Modal>}</Layout>}
function Access({kind}:{kind:'tenant'|'provider'|'guest'}){const code=kind==='tenant'?'ML-A5G-7284':kind==='provider'?'ML-PROV-5820':'ML-GRACE-4931';return <Layout eyebrow={`${kind} access`} title="Access pass" text="Your credential, property and current access status."><section className="access-code-card"><span className="eyebrow">Active credential</span><strong>{code}</strong><img src={`https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=${code}`} alt="QR credential"/><small>Riverside Apartments · Building A · Nairobi</small><a className="button button--secondary button--full" href={map('Building A Riverside Apartments Nairobi')} target="_blank" rel="noreferrer">View directions</a></section></Layout>}
function Verify(){const[c,setC]=useState(''),[ok,setOk]=useState<any>(null);const go=()=>{const r=access().find((x:any)=>x.code===c.trim().toUpperCase());setOk(r||false)};return <Layout eyebrow="Security desk" title="Verify access" text="Check a QR/code credential before admitting a visitor."><section className="role-panel"><Field label="Access code" icon="key" value={c} set={setC} placeholder="e.g. ML-GRACE-4931"/><button className="button button--primary" onClick={go}>Verify credential</button>{ok&&<div className="verify-result valid"><Icon name="check"/><div><strong>{ok.name}</strong><span>{ok.property} · {ok.unit||'Property'} · {ok.state}</span></div></div>}{ok===false&&<div className="verify-result invalid"><Icon name="close"/><div><strong>Access denied</strong><span>Credential not found.</span></div></div>}</section></Layout>}
const data=(role:Role,section:string):Item[]=>{if(role==='provider'){if(section==='my-jobs')return[{title:'Plumbing repair · Unit A5G',meta:'Building A · Riverside Apartments · Today 4:00–5:00 PM',status:'Accepted',icon:'tool',image:service},{title:'Electrical inspection · Unit B2',meta:'Building B · Tomorrow 11:00 AM',status:'Pending',icon:'tool'}];if(section==='my-services')return[{title:'Plumbing & leak repair',meta:'Verified · Nairobi · 4.9 rating',status:'Active',icon:'tool',image:service},{title:'Emergency maintenance',meta:'Response window · 60 min',status:'Enabled',icon:'shield'}];return[{title:'Next job · A5G',meta:'Building A · Today 4:00 PM',status:'Confirmed',icon:'calendar'},{title:'Access pass · ML-PROV-5820',meta:'Building A · Active',status:'Active',icon:'key'}]};if(role==='guest')return[{title:'Reception desk',meta:'Open 07:00–22:00 · visitor assistance',status:'Open',icon:'users'},{title:'Visitor parking',meta:'Gate A · Building A',status:'Available',icon:'building'},{title:'Security desk',meta:'Verify QR at arrival',status:'Ready',icon:'shield'}];return[{title:'Amina Njoki',meta:'Tenant · Building A · A5G',status:'Active',icon:'users'},{title:'Grace Wanjiku',meta:'Guest · Building A · Today',status:'Inside',icon:'key'},{title:'Moses K.',meta:'Provider · Building A · Plumbing',status:'Expected',icon:'tool'}]}
function Generic({role,section}:{role:Role;section:string}){const titles:Record<string,string>={'my-reports':'My Reports','services':'Services','security':'Security','people-presence':'People & Presence','visitor-access':'Visitor Access','requests':'Requests','service-providers':'Service Providers','service-visits':'Service Visits','my-jobs':'My Jobs','my-services':'My Services','access-guide':'Access Guide','visit-details':'Visit Details'};const title=titles[section]||section.replaceAll('-',' ');return <Layout eyebrow={role==='tenant'?'Resident workspace':role==='provider'?'Service provider':role==='guest'?'Guest workspace':'Management workspace'} title={title.charAt(0).toUpperCase()+title.slice(1)} text="Role-aware workspace with realistic presentation data and interactive records." action={<Action label="Add new"/>}><Stats v={[["Active","12","Current"],["Pending","4","Needs attention"],["Today","8","Scheduled"]]}/><section className="role-panel"><List items={data(role,section)}/></section>{(section==='service-visits'||section==='my-jobs')&&<article className="role-photo-card"><img src={service} alt="Service work"/><div><span className="eyebrow">Property location</span><h3>Building A · Riverside Apartments</h3><p>Plumbing service appointment · Unit A5G · Nairobi</p><a className="button button--secondary button--small" href={map('Building A Riverside Apartments Nairobi')} target="_blank" rel="noreferrer">View directions on maps</a></div></article>}</Layout>}
export function RoleSection({role,section='overview'}:{role:Role;section?:string;onNavigate?:(label:string)=>void}){if(role==='tenant'){if(section==='home'||section==='overview')return <TenantHome/>;if(section==='my-access')return <Access kind="tenant"/>;if(section==='visitors')return <Visitors/>;if(section==='calendar')return <Calendar/>;return <Generic role={role} section={section}/>}if(role==='provider'){if(section==='access-pass')return <Access kind="provider"/>;if(section==='calendar'||section==='schedule')return <Calendar/>;return <Generic role={role} section={section}/>}if(role==='guest'){if(section==='access-pass')return <Access kind="guest"/>;if(section==='calendar')return <Calendar/>;return <Generic role={role} section={section}/>}if(section==='verify-access')return <Verify/>;if(section==='calendar')return <Calendar/>;return <Generic role={role} section={section}/>}