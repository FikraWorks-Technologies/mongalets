import { useState } from 'react'
import { Icon } from '../components/Icon'
import './visitor-invite.css'
export function VisitorInvitePage({tenantCode}:{tenantCode:string}){const [name,setName]=useState(''),[phone,setPhone]=useState(''),[date,setDate]=useState(''),[time,setTime]=useState(''),[minors,setMinors]=useState(0),[done,setDone]=useState(false);return <div className="public-invite"><div className="public-invite-card"><div className="public-invite-brand"><span>ML</span><div><strong>MongaLets</strong><small>Secure visitor invitation</small></div></div><div className="public-invite-hero"><span className="eyebrow">Tenant-linked invitation</span><h1>Tell us about your visit</h1><p>This secure form is linked to the resident who invited you. Complete your details so the property team can prepare your access.</p></div>{done?<div className="invite-success"><Icon name="check" size={28}/><h2>Details submitted</h2><p>Your visitor information has been received.</p><small>Invitation reference: {tenantCode}</small></div>:<><div className="ml-form-grid"><label className="ml-access-field"><span>Full name</span><div><Icon name="users" size={17}/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name"/></div></label><label className="ml-access-field"><span>Mobile number</span><div><Icon name="phone" size={17}/><input type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="e.g. 0712 345 678"/></div></label><label className="ml-access-field"><span>Visit date</span><div><Icon name="calendar" size={17}/><input value={date} onChange={e=>setDate(e.target.value)} placeholder="e.g. 30 Aug 2026"/></div></label><label className="ml-access-field"><span>Arrival time</span><div><Icon name="calendar" size={17}/><input value={time} onChange={e=>setTime(e.target.value)} placeholder="e.g. 2:30 PM"/></div></label></div><div className="group-counter"><button onClick={()=>setMinors(Math.max(0,minors-1))}>−</button><strong>{minors}</strong><button onClick={()=>setMinors(minors+1)}>+</button><span>accompanying minors</span></div><div className="role-callout"><Icon name="shield" size={18}/><div><strong>Your details are linked to this invitation</strong><span>Minors do not need separate credentials and remain attached to the accompanying adult.</span></div></div><button className="button button--primary button--full" disabled={!name.trim()||!phone.trim()||!date.trim()||!time.trim()} onClick={()=>setDone(true)}>Submit visitor details</button></>}</div></div>}
