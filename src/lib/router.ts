import { useEffect, useState } from 'react'
import type { Role, Route } from './types'
const roles:Role[]=['owner','tenant','provider','guest','admin']
export function parseRoute(pathname=window.location.pathname):Route{if(pathname==='/')return{page:'home'};if(pathname==='/login')return{page:'login'};const invite=pathname.match(/^\/invite\/([A-Za-z0-9-]+)$/);if(invite)return{page:'invite',role:'tenant',section:invite[1]};const login=pathname.match(/^\/login\/(owner|tenant|provider|guest|admin)$/);if(login)return{page:'login',role:login[1] as Role};const portal=pathname.match(/^\/app\/(owner|tenant|provider|guest|admin)(?:\/([a-z0-9-]+))?$/);if(portal&&roles.includes(portal[1] as Role))return{page:'portal',role:portal[1] as Role,section:portal[2]};return{page:'not-found'}}
export function navigate(path:string){if(window.location.pathname===path)return;window.history.pushState({},'',path);window.dispatchEvent(new PopStateEvent('popstate'));window.scrollTo({top:0,behavior:'smooth'})}
export function useRoute(){const[route,setRoute]=useState<Route>(()=>parseRoute());useEffect(()=>{const onChange=()=>setRoute(parseRoute());window.addEventListener('popstate',onChange);return()=>window.removeEventListener('popstate',onChange)},[]);return route}
