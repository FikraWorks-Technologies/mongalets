import { useState } from 'react'
import { Icon } from './Icon'
import type { Role, Session } from '../lib/types'

type FamilyMember={id:string;name:string;relationship:string;phone:string;minor:boolean;notes:string;photo?:string}
type Profile={phone:string;location:string;unit:string;property:string;bio:string;photo?:string;family:FamilyMember[]}
const KEY='mongalets.role-profiles.v1'
const read=():Record<string,Profile>=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return {}}}
const base=(role:Role):Profile=>({phone:'',location:'Nairobi, Kenya',unit:role==='tenant'?'A5G':'',property:role==='tenant'?'Building A':'',bio:'',family:[]})
export const getRoleProfile=(s:Session)=>read()[s.role]||base(s.role)
const initials=(n:string)=>n.split(' ').filter(Boolean).map(x=>x[0]).slice(0,2).join('').toUpperCase()||'ML'
export function RoleProfilePanel({role,session}:{role:Role;session:Session}){
 const [p,setP]=useState(()=>getRoleProfile(session)),[name,setName]=useState(session.name),[email,setEmail]=useState(session.email),[open,setOpen]=useState(false),[saved,setSaved]=useState(false)
 const [m,setM]=useState<FamilyMember>({id:'',name:'',relationship:'Family member',phone:'',minor:false,notes:''})
 const save=(next=p)=>{setP(next);const all=read();all[role]=next;localStorage.setItem(KEY,JSON.stringify(all));window.dispatchEvent(new CustomEvent('mongalets:profile',{detail:next}));setSaved(true);setTimeout(()=>setSaved(false),1800)}
 const add=()=>{if(!m.name.trim()||p.family.length>=10)return;save({...p,family:[...p.family,{...m,id:`FM-${Date.now()}`}]});setM({id:'',name:'',relationship:'Family member',phone:'',minor:false,notes:''});setOpen(false)}
 const photo=(f?:File)=>{if(!f)return;const r=new FileReader();r.onload=()=>save({...p,photo:String(r.result)});r.readAsDataURL(f)}
 const label=role==='owner'?'Management':role==='admin'?'Security administrator':role==='tenant'?'Resident / tenant':role==='provider'?'Service provider':'Guest'
 const details=role==='tenant'?[['Permanent access','ML-A5G-7284'],['Property',p.property||'Building A'],['Unit',p.unit||'A5G'],['Family',`${p.family.length}/10 linked`]]:role==='provider'?[['Business',session.name],['Service area',p.location],['Contact',p.phone||'Not added'],['Account','Verified provider']]:role==='owner'?[['Workspace','Management'],['Scope','Authorised properties'],['Contact',p.phone||'Not added'],['Access','Owner / management']]:role==='admin'?[['Workspace','Security administration'],['Coverage','Building security'],['Contact',p.phone||'Not added'],['Access','Security admin']]:[['Visit','Approved guest'],['Host','MongaLets resident'],['Contact',p.phone||'Not added'],['Credential','Personal access pass']]
 return <section className="role-profile-page"><div className="role-section-head"><div><span className="eyebrow">Account · {label}</span><h1>My profile</h1><p>Your identity and role-specific details used across the MongaLets workspace.</p></div><button className="button button--primary" onClick={()=>save()}><Icon name="check" size={16}/> Save changes</button></div><div className="profile-layout"><section className="role-panel profile-card"><div className="profile-hero"><div className="profile-large-avatar">{p.photo?<img src={p.photo} alt={name}/>:<span>{initials(name)}</span>}</div><div><span className="eyebrow">Profile</span><h2>{name}</h2><p>{label}</p></div></div><label className="profile-upload">Change profile photo<input type="file" accept="image/*" onChange={e=>photo(e.target.files?.[0])}/></label><div className="profile-fields"><label>Full name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Mobile number<input value={p.phone} onChange={e=>setP({...p,phone:e.target.value})} placeholder="e.g. 0712 345 678"/></label><label>Location<input value={p.location} onChange={e=>setP({...p,location:e.target.value})}/></label></div><label className="profile-wide">About / notes<textarea rows={4} value={p.bio} onChange={e=>setP({...p,bio:e.target.value})}/></label>{saved&&<div className="success-note"><Icon name="check" size={16}/> Saved.</div>}</section><section className="role-panel"><div className="role-panel-head"><div><h2>Role details</h2><p>Information relevant to this account.</p></div></div><div className="profile-detail-grid">{details.map(([a,b])=><div key={a}><small>{a}</small><strong>{b}</strong></div>)}</div>{role==='tenant'&&<><div className="profile-family-head"><div><h2>Family & permanent access</h2><p>Link up to 10 family members. Minors count toward the same 10.</p></div><span className="family-count">{p.family.length}/10</span></div><div className="family-list">{p.family.map(x=><article key={x.id}><div className="family-avatar">{initials(x.name)}</div><div><strong>{x.name}</strong><small>{x.relationship} · {x.minor?'Minor':'Adult'}{x.phone?` · ${x.phone}`:' · No mobile number'}</small>{x.notes&&<span>{x.notes}</span>}</div><button className="icon-button" onClick={()=>save({...p,family:p.family.filter(y=>y.id!==x.id)})}><Icon name="close" size={15}/></button></article>)}{!p.family.length&&<div className="profile-empty"><Icon name="users" size={20}/><strong>No family members yet</strong><span>Add people to your permanent access profile.</span></div>}</div><button className="button button--secondary button--small" disabled={p.family.length>=10} onClick={()=>setOpen(true)}>+ Add family member</button></>}</section></div>{open&&<div className="ml-modal-backdrop"><div className="ml-modal"><div className="role-panel-head"><div><span className="eyebrow">Permanent access</span><h2>Add family member</h2><p>Every member counts toward the 10-member limit.</p></div><button className="icon-button" onClick={()=>setOpen(false)}><Icon name="close"/></button></div><div className="ml-form-grid"><label className="ml-access-field"><span>Full name</span><div><Icon name="users" size={17}/><input value={m.name} onChange={e=>setM({...m,name:e.target.value})} placeholder="e.g. David Otieno"/></div></label><label className="ml-access-field"><span>Relationship</span><div><Icon name="home" size={17}/><input value={m.relationship} onChange={e=>setM({...m,relationship:e.target.value})} placeholder="Spouse, son, daughter"/></div></label><label className="ml-access-field"><span>Mobile number</span><div><Icon name="phone" size={17}/><input value={m.phone} onChange={e=>setM({...m,phone:e.target.value})} placeholder="Optional for minors"/></div></label><label className="ml-access-field"><span>Notes</span><div><Icon name="file" size={17}/><input value={m.notes} onChange={e=>setM({...m,notes:e.target.value})} placeholder="Optional"/></div></label></div><label className="profile-check"><input type="checkbox" checked={m.minor} onChange={e=>setM({...m,minor:e.target.checked})}/> This member is a minor</label><div className="role-callout"><Icon name="shield" size={18}/><div><strong>{10-p.family.length} slot{10-p.family.length===1?'':'s'} remaining</strong><span>Minors may be stored without a phone number. Their full profile remains linked to your permanent access.</span></div></div><div className="security-modal-actions"><button className="button button--secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button button--primary" disabled={!m.name.trim()} onClick={add}>Add to permanent access</button></div></div></div>}</section>
}
